# Recharts and a few Radix packages still declare a React 18 peer range; we run
# React 19 deliberately, so don't let those warnings fail the install.
strict-peer-dependencies=false
auto-install-peers=true
